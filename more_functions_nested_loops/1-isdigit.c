#include "main.h"
/**
 * _isdigit - verifie les caractere en majuscules (0 through 9)
 * @c: int nombre entier
 * Return: 1 si c est un chiffre sinon 0
 **/
int _isdigit(int c) // int nombre entier de la function _isdigit
{
return (c >= '0' && c <= '9'); // && et 
}
