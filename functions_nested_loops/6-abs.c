#include "main.h"
/**
 *abs - computes the absolute value of an integer
 *
 *
 * return: integer.
 **/
int _abs(int value)
{
if (value < 1)
return (value * -1);
else
return (value = 1);

return (value);
}
