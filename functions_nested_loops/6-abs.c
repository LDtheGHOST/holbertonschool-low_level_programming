#include "main.h"
/**
 * _abs - computes the absolute value of an integer
 * @value : the value that compute
 *
 * return: integer.
 **/
int _abs(int value)
{
if (value < 1)
return (value * -1);
else
return (value = 1);

return (value);
}
