#include "main.h"
/**
 * isalpha - checks for alphabetic character
 *
 * Return: 0.
 **/
int _isalpha(int c)
{
if (('a' <= c && c <= 'z') || ('A' <= c && c <= 'Z'))
{
return (1);
}
else
{
return (0);
}

return (0);
}
